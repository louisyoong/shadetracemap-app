// Template for lib/app_config.dart, which is gitignored so app-specific
// identifiers don't sit in the public repo. Copy this file to
// lib/app_config.dart and fill in the real value.
//
// Find the App Store numeric ID at:
// App Store Connect -> your app -> App Information -> "Apple ID" field
// (looks like 1234567890 - the "id" prefix is added automatically below).
const appStoreId = 'REPLACE_WITH_APP_STORE_ID';
